Need a lambda python code that should get the Event record from the event history for every create function, create bucket, create service event name it should fetch only the user identity type, principleID, arn, session issuer user name and also it should check that session issuer user name have tag or not from IAM Role if yes then it should show how many tags are available, user name using json. loads and function name, bucket name, service name and it  should fetch those functions IAM role name and also need to che ch whether that IAM roles have tag or not and it should generate in table format and that table need to send in mail using ses
