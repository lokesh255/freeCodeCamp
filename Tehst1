Need a lambda python code that should get the Event record from the event history for every createfunction ,create bucket event name it should fetch the resources name, resource type from detail and also it should fetch only the user identity type, principleID, arn, session issuer user name and also it should check that session issuer user name and function name, bucket name, service name and it should generate in table format and bucket name, function name ahould be in same row called as resource name and that table need to send in mail using ses
