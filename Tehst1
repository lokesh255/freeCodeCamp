Need a lambda python code that should get the Event record from the event history for every create function event name it should fetch only the user identity type, principleID, arn, session issuer role arn, user name using json. loads and function name, and it  should enter into response elements and fetch the functionsArn,  role name and also need to check whether that IAM roles have tag or not and it should generate in table format and that table need to send in mail using ses
